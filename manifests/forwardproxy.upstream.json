{
    "name": "forwardproxy",
    "kind": "binary",
    "version": "naive-branch",
    "upstream": "https://github.com/klzgrad/forwardproxy",
    "verify": {
        "command": ["caddy", "list-modules"],
        "expect_stdout_contains": "http.handlers.forward_proxy",
        "expect_zero_exit": true
    },
    "note": "Apache-2.0. NaiveProxy fork of the Caddy forwardproxy plugin. The 'naive-branch' string is symbolic — we pin the upstream commit via xcaddy in the Caddy Dockerfile, not by version number, because klzgrad's branch isn't tagged. The OK/NG check confirms the module is loaded into the running Caddy."
}
