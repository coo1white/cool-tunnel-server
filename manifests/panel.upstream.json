{
    "name": "panel",
    "kind": "container-image",
    "version": "0.5.0",
    "upstream": "https://github.com/coo1white/cool-tunnel-server",
    "verify": {
        "command": ["bun", "run", "/opt/cool-tunnel/operator/src/index.ts", "admin", "doctor"],
        "expect_zero_exit": true
    },
    "note": "AGPL-3.0-only Bun/Hono/Better Auth admin panel code (Copyright (C) 2026 coolwhite LLC). Built from docker/panel/Dockerfile. Verify runs the Bun admin doctor command inside the panel container and fails if admin config, SQLite auth storage, or the Rust boundary is unhealthy."
}
