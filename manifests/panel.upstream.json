{
    "name": "panel",
    "kind": "container-image",
    "version": "0.0.1",
    "upstream": "https://github.com/coo1white/cool-tunnel-server",
    "verify": {
        "command": ["docker", "exec", "ct-panel", "php", "artisan", "--version"],
        "expect_stdout_contains": "Laravel Framework",
        "expect_zero_exit": true
    },
    "note": "Apache-2.0 (panel code) + MIT (Laravel + Filament). Built from docker/panel/Dockerfile. Verify hits the running container's artisan to confirm Laravel boot."
}
