{
    "name": "redis",
    "kind": "container-image",
    "version": "7.4.8",
    "upstream": "https://hub.docker.com/_/redis",
    "verify": {
        "command": ["bash", "-c", "REDISCLI_AUTH=\"$REDIS_PASSWORD\" redis-cli -h \"$REDIS_HOST\" -p \"$REDIS_PORT\" INFO Server | grep -i ^redis_version:"],
        "expect_zero_exit": true
    },
    "note": "BSD-3. Retained for runtime/cache compatibility where existing core functionality still requires it. Verify runs `redis-cli INFO Server` and checks the pinned version. Auth is mandatory; REDISCLI_AUTH keeps the password out of argv."
}
