{
    "name": "ct-server-core",
    "kind": "binary",
    "version": "0.5.0",
    "upstream": "https://github.com/coo1white/cool-tunnel-server",
    "verify": {
        "command": ["ct-server-core", "version"],
        "expect_stdout_contains": "ct-server-core",
        "expect_zero_exit": true
    },
    "note": "AGPL-3.0-only (Copyright (C) 2026 coolwhite LLC). The Rust engine that the Bun admin/operator layer calls for sing-box config generation, runtime checks, metrics scraping, quota enforcement, anti-tracking probes, and burst coalescing. Bumping this is a normal release."
}
