{
    "name": "mariadb",
    "kind": "container-image",
    "version": "11.8.6",
    "upstream": "https://hub.docker.com/_/mariadb",
    "verify": {
        "command": ["bash", "-c", "MYSQL_PWD=\"$DB_PASSWORD\" mariadb -h \"$DB_HOST\" -P \"$DB_PORT\" -u \"$DB_USERNAME\" -BN -e 'SELECT VERSION()'"],
        "expect_zero_exit": true
    },
    "note": "GPL-2.0. Retained only for core runtime data that still requires MariaDB. Admin/account state uses SQLite by default. Verify runs `SELECT VERSION()` using the existing DB user; MYSQL_PWD keeps the password out of argv."
}
