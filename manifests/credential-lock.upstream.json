{
    "name": "credential-lock",
    "kind": "binary",
    "version": "db=rendered=manifest=mac-config",
    "upstream": "https://github.com/coo1white/cool-tunnel-server",
    "verify": {
        "command": ["php", "/var/www/html/artisan", "credential-lock:check"],
        "expect_stdout_contains": "db=rendered=manifest=mac-config",
        "expect_zero_exit": true
    },
    "note": "Deployment invariant guard. Reads active proxy_accounts from MariaDB, parses the rendered /data/config/singbox.json users array, and fetches each active subscription manifest through the panel route. Fails if username/UUID tuples differ across DB, rendered sing-box config, and subscription output. It never prints UUIDs; mismatch diagnostics name missing/extra usernames only."
}
