{
    "name": "credential-lock",
    "kind": "binary",
    "version": "db=rendered=manifest=mac-config",
    "upstream": "https://github.com/coo1white/cool-tunnel-server",
    "verify": {
        "command": ["bun", "run", "/opt/cool-tunnel/operator/src/index.ts", "admin", "doctor"],
        "expect_zero_exit": true
    },
    "note": "Deployment invariant guard owned by the Bun operator/admin surface. The doctor path validates admin configuration and the rendered runtime boundary without printing secrets; mismatch diagnostics must name components and next commands, not UUIDs or tokens."
}
