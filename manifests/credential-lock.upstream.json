{
  "name": "credential-lock",
  "kind": "binary",
  "version": "db=rendered=manifest=mac-config",
  "upstream": "https://github.com/coo1white/cool-tunnel-server",
  "verify": {
    "command": ["ct", "doctor"],
    "expect_zero_exit": true
  },
  "note": "Deployment invariant guard. This is enforced by ct doctor plus the admin-api render boundary: active proxy_account rows in SQLite, rendered /data/config/singbox.json users, and subscription output must agree. Diagnostics must not print UUIDs or subscription tokens; mismatch messages should name missing/extra usernames only."
}
